---
export const prerender = false;

/**
 * There is no admin home page. The queue is the product, so the root goes
 * straight to it rather than presenting a landing screen somebody has to click
 * through every time.
 */
return Astro.redirect('/submissions', 302);
---
