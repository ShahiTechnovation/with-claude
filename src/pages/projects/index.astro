---
import Base from '@/layouts/Base.astro';
import Section from '@/components/Section.astro';
import PageIntro from '@/components/PageIntro.astro';
import ProjectArchive from '@/components/ProjectArchive.astro';
import SubmitPanel from '@/components/SubmitPanel.astro';
import { publicProjects } from '@/data';
---

<Base
  title="Projects"
  description="Made with Claude — an archive of what people across India have actually built."
  active="/projects"
>
  <PageIntro
    eyebrow="Projects"
    title="Made with Claude."
    stamp={`${String(publicProjects.length).padStart(2, '0')} archived`}
  >
    <p>
      Things people across India are actually making. Every prototype from a build day, every
      weekend project, every thing someone shipped after a workshop — with how Claude was actually
      used, which is the part worth reading.
    </p>
  </PageIntro>

  <Section id="archive" tone="deep" size="lg" labelledby="projects-heading">
    <ProjectArchive heading="The archive" eyebrow="Archive" filters />
  </Section>

  <Section id="submit" labelledby="build-heading">
    <SubmitPanel formId="build" />
  </Section>
</Base>
