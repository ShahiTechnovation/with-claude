import type { Project } from './types';

/**
 * MADE WITH CLAUDE — the project archive.
 *
 * Deliberately empty. The Bhopal organisers report 34+ prototypes shipped,
 * but none of them are documented anywhere citable: no titles, no builders,
 * no links. Publishing invented projects would poison the most valuable
 * long-term asset this site has.
 *
 * The archive UI is complete and renders its open state from this array being
 * empty. Add one verified project and the grid takes over.
 *
 * TODO: collect real submissions (title, builder, city, link, how Claude was
 * used) from the Impact Lab and workshop demo sessions.
 */
export const projects: Project[] = [];
