import type { Project } from './types';

/**
 * The project archive — BUILT WITH CLAUDE.
 *
 * Deliberately empty. The Bhopal chapter reports 34+ prototypes shipped, but
 * none of them are documented anywhere we can cite: no names, no builders, no
 * links. Publishing invented projects would poison the most valuable long-term
 * asset this site has.
 *
 * The archive UI is fully built and renders its open-call state from this
 * array being empty. Add one verified project and the grid takes over.
 *
 * TODO: collect real submissions (title, builder, city, link, how Claude was
 * used) from the Impact Lab and workshop demo sessions.
 */
export const projects: Project[] = [];
